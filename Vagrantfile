

Vagrant.configure(2) do |config|  
  config.vm.box = "hashicorp/precise64"
  config.vm.provider "hyperv"
  config.vm.network "public_network"
end



Vagrant.configure(2) do |config|  
  config.vm.box = "dist/hyperv-ubuntu-bionic.box"
  config.vm.provider "hyperv"
  config.vm.network "public_network"
end

